diff --git a/dist/parser/flowchart.js b/dist/parser/flowchart.js
index 5713001..3bbf045 100644
--- a/dist/parser/flowchart.js
+++ b/dist/parser/flowchart.js
@@ -104,7 +104,11 @@ const parseSubGraph = (data, containerEl, classes) => {
         return n;
     });
     // Get position
-    const el = containerEl.querySelector(`[id='${data.id}']`);
+    // Mermaid >= 11 prefixes rendered cluster ids with the render id
+    // (`<renderId>-<subgraphId>`), so the raw id only matches on older versions.
+    const svgId = containerEl.querySelector("svg")?.getAttribute("id");
+    const el = containerEl.querySelector(`[id='${data.id}']`) ||
+        (svgId ? containerEl.querySelector(`[id='${svgId}-${data.id}']`) : null);
     if (!el) {
         throw new Error("SubGraph element not found");
     }
